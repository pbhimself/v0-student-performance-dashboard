// Optional small hero note component to reuse if needed later
export default function HeroNote() {
  return (
    <div className="rounded-md border p-3 text-sm text-muted-foreground">
      Upload Excel with a Name column and any number of subject columns. Add a "Previous" sheet to compare progress. All
      processing happens locally in your browser.
    </div>
  )
}
